# 🦸 Rico Daily Standup — 2026-09-04 (Friday)
**To:** CEO (Muzaffar) · **From:** Atlas, on behalf of the team

## TL;DR
Found the app down live at the start of this run — `/api/health` and `/api/board` both failing with a
Supabase DNS NXDOMAIN, same signature as the 2026-08-16 incident. This time we diagnosed the actual
root cause instead of just flagging it: Supabase free-tier projects auto-pause after 7 days with zero
database activity, and since GTM hasn't started, organic traffic alone isn't reliable enough to prevent
that. Shipped two fixes today: a daily Vercel Cron ping that keeps the project awake permanently (code
complete, awaiting deploy approval), and the "honest outage banner" Nova proposed 2026-08-17 so the
*next* degradation — whatever causes it — is visible to users instead of silent. Board and stats were
unreachable all run since both depend on the same downed DB.

## 🧭 Strategy — Sage
**GTM stays held — now for two stacked reasons, one newly confirmed.** Yesterday's Anthropic-credits
concern is unverified either way today (every DB-dependent route was unreachable, so there was no way to
re-check `/api/tony` actually generates). On top of that, the Supabase outage is back. Recommendation,
unchanged in direction but sharper: don't start the 30-day push until BOTH `/api/health` reads `ok:true`
AND a real generate is confirmed on `/api/tony` — two independent outages in three weeks is exactly the
"broken core loop" scenario Sage has flagged since 2026-07-22, and a GTM push is the one thing that can't
afford to launch onto it twice. Separately: this is the second time in ~7 weeks a $0 free-tier limitation
has cost a full standup cycle and a manual CEO dashboard trip. Today's cron fix (below) should make this
a non-issue going forward at zero cost — recommend holding off on upgrading to Supabase Pro until we've
seen at least one full cycle of the cron actually preventing a repeat, rather than paying to fix something
a free mechanism now handles.

## 🔬 R&D — Nova
1. **Shipped today:** honest degraded-service banner (carried from 2026-08-17 idea #1) — see Shipped below.
2. Carried: **Anthropic reachability check on `/api/health`** (S) — still the natural next step; today's
   new `/api/cron/keepalive` endpoint already proves the pattern (a cheap scheduled ping), so extending
   `/api/health` itself to also probe Anthropic is now mostly copy-paste of code that exists twice over.
3. Carried: **digest-driven re-engagement** (M, 2026-08-16) — still needs a design pass, not built yet.

## 📋 Board
**Unreachable all run** — `/api/board` depends on the same Supabase connection that was down, so no
board tasks could be read or moved today. Last known state (2026-08-17): all 8 team-assigned cards
`done`; 3 CEO-assigned cards (Stripe, testimonials, GTM push) still `todo`, unchanged since 2026-06-30
(66 days) — reminders below, not re-litigated. Will re-read the board as soon as the DB is confirmed
back up.

## 🎯 The idea we're working on
Reliability plumbing: turning "the CEO happens to notice something's broken" into automatic detection
(the banner) and automatic prevention (the cron) — so by the time a GTM push starts, the app is
trustworthy enough to actually run one, and the team isn't burning build cycles re-discovering the same
outage every few weeks.

## 📌 Goals
D7 retention ≥18% (Aug 2026 cohort) · 500 registered users · ≥60% activation · ≥3% free→paid post-Stripe.
No `/api/stats` read possible today (DB unreachable) — will re-pull the moment it's back.

## ✅ Shipped today
- **Root-caused the recurring Supabase outage:** confirmed via Supabase's own docs that free-tier
  projects auto-pause after 7 days of zero DB requests — the same DNS-NXDOMAIN signature seen live
  today matches the 2026-08-16 incident exactly. With GTM not yet started, there isn't enough organic
  traffic to keep the 7-day timer from lapsing on its own.
- **`fix/supabase-keepalive-cron`** — added `pages/api/cron/keepalive.js` (one cheap read via the
  existing `sb()` helper) + a `crons` entry in `vercel.json` that hits it once daily, using Vercel's
  native Cron feature (Hobby-plan compatible at daily frequency) rather than a new external automation
  layer. Resets the pause timer with 6 days of margin, for $0. Returns a non-200 on failure so a real
  outage shows up in Vercel's own cron-invocation logs as a bonus early-warning signal. `next build`
  passes clean. Branch pushed, PR opened
  (https://github.com/ALIMUZAFFARMOHAMMAD/rico/pull/8), merged into `safety/working-tree-2026-06-30`,
  also pushed. ⏳ Awaiting CEO deploy approval — this only prevents the *next* pause, it does not fix
  today's live outage (that needs the CEO to un-pause the project in the Supabase dashboard, same
  manual step as 2026-08-16). (Forge)
- **`feature/status-degraded-banner`** — built Nova's 2026-08-17 idea: `components/StatusBanner.js`
  polls `/api/health` every 60s and shows a small, dismissible, non-alarming banner ("Some features are
  briefly paused — we'll be right back.") whenever the backend is degraded, wired globally via
  `pages/_app.js`. Directly motivated by two real incidents now on record (2026-08-16 Anthropic credits,
  today's Supabase pause) where users would have seen a companion that silently stopped saying anything
  new. `next build` passes clean. Branch pushed, PR opened
  (https://github.com/ALIMUZAFFARMOHAMMAD/rico/pull/7), merged into `safety/working-tree-2026-06-30`,
  also pushed. ⏳ Awaiting CEO deploy approval. (Forge)
- Fixed a git-credential snag hit mid-run: `gh` was authenticated as `Muzaffar-07` but git push over
  HTTPS was using stale cached credentials rather than `gh`'s token. Ran `gh auth setup-git` to wire
  git to use the `gh` credential helper — both pushes above went through cleanly afterward. No code
  impact, just a sandbox auth fix future runs shouldn't need to repeat.

## 📣 Content & social — Echo
No new draft — today's build is backend-only, nothing screen-recordable. The Club Feed launch post
(`CONTENT_CALENDAR.md`, src=ig6) is still sitting unapproved — still the strongest "it's alive" content
whenever you want it live.

## 🎬 Video — Reel
Nothing new today. The investor walkthrough video (`DEMO_SCRIPT.md`) is still unrecorded — carried.

## 📊 Metrics
No read possible — `/api/stats` depends on the same downed Supabase connection as everything else today.
Last real numbers remain 2026-08-16's (n=6): 50% 2+-day retention, 33% activation. Will re-pull the
moment the DB is confirmed back up.

## 🚧 Challenges
- **Live Supabase outage for the full run** — diagnosed and root-caused (see Shipped), but not fixable
  from this sandbox; needs the CEO to un-pause the project in the Supabase dashboard, same as 2026-08-16.
- Because of the outage, neither of today's two shipped fixes could be exercised end-to-end locally
  (both depend on `sb()`, which was unreachable) — verification is build-clean + code review only, same
  caveat as most fixes over the past few weeks.
- Standing local-dev gap, unchanged: `CLERK_SECRET_KEY` / `SUPABASE_SERVICE_ROLE_KEY` are blank in this
  sandbox's `.env.local`, so DB/Clerk-backed flows can only be verified against production, not locally.
- Anthropic-credits status from yesterday's flag remains unverified either way — the DB outage blocked
  every route that would have proven it one way or the other.

## ⚠️ Needs your approval
1. **🚨 Un-pause the Supabase project again** at supabase.com/dashboard (same 2-minute manual step as
   2026-08-16) — this is the live blocker on `/api/health`, `/api/board`, `/api/stats`, and every
   DB-backed feature right now.
2. **Deploy `fix/supabase-keepalive-cron` + `feature/status-degraded-banner`** — both low-risk, both
   code-complete today, bundle into one deploy.
3. Carried: **🚨 Confirm Anthropic credits are restored** at console.anthropic.com (flagged 2026-08-16,
   status unverified today due to the Supabase outage) — the highest-leverage single action once #1 is
   done, since it's the other half of the "is the core loop actually healthy" question blocking GTM.
4. Carried: **`feature/hubspot-signup-sync` — is this still wanted?** Undocumented, now ~9-weeks-stale
   branch that would sync every new signup to HubSpot.
5. Carried, partially addressed: **external uptime alerting** — today's banner covers the in-app signal
   and the cron covers prevention, but there's still no alert that reaches *you* if health flips red
   again. Still asking before building that, since it's a standing automation, not a one-off code change.
6. Carried: **Record/generate the investor walkthrough video** (`DEMO_SCRIPT.md`).
7. Carried: **Approve the Club Feed launch post** (`CONTENT_CALENDAR.md`, src=ig6).
8. REMINDER (unchanged since 2026-06-30, 66 days): Plan gating + Stripe, Lock 3 named testimonials,
   Start 30-day GTM push — hold #3 specifically until #1 and #3 above both confirm healthy.

⚠️ email not sent — Zapier's Gmail connection is stale and needs reconnecting (both `gmail_send_email`
and `gmail_create_draft` failed with the same auth error: "the default connection set for Gmail is
stale"). Same underlying issue as prior runs' notes on this connector. Approval items are listed above
in this file — reconnect the Zapier Gmail integration at zapier.com when convenient so future runs can
alert you directly.

## 🔜 Tomorrow's plan
- Once the Supabase project is un-paused: confirm the keepalive cron fired (Vercel dashboard → Cron
  Jobs), re-read `/api/board` and `/api/stats`, and re-verify `/api/tony`/`/api/checkin`/`/api/club-feed`
  actually generate (not just return 200) before touching the GTM recommendation.
- If Anthropic credits are also confirmed restored: revisit the GTM push call directly — that closes
  both open conditions at once.
- Otherwise: extend `/api/health` with the Anthropic reachability check (Nova idea #2, now a near-copy
  of today's cron pattern) as the next-smallest buildable item.

# 🦸 Rico Daily Standup — 2026-07-04
**To:** CEO (Muzaffar) · **From:** Atlas, on behalf of the team

## TL;DR
Two things landed today: a Memory Vault **data-export button** ("download everything Rico
remembers" — GDPR-friendly, extends the trust story) shipped to a branch earlier today, and — after
recommending it twice without acting — the team finally wrote the **2-min investor demo script**
(`standups/DEMO_SCRIPT.md`) plus a matching Reel video spec. Added a sharp retention benchmark to the
investor narrative. The same three CEO-owned board tasks (Stripe, testimonials, GTM push) are now
unchanged for 5 days — still the real bottleneck, flagged again below.

## 🧭 Strategy — Sage
Pulled fresh market data: Character.AI (category leader) runs ~50–60% D1, ~30% D7, and **13–18% D30**
retention. Rico's own North Star is D7 ≥18% — our one-week target already matches the leader's
one-month range. That's a sharp, defensible line for the raise once real data lands: "our week-one
retention target matches best-in-class month-one retention in this category." Added to
`STRATEGY.md` §4 with sourcing. Recommendation: the CEO board tasks (Stripe/testimonials/GTM) are
unchanged since 2026-06-30 — pick at least one this week; GTM push is the cheapest to start (zero
engineering dependency) and unblocks the metric story fastest. Full log in `STRATEGY.md` §7.

## 🔬 R&D — Nova
- **In-app testimonial capture flow** (S, new) — consent-gated in-app prompt so opted-in beta users
  can submit a short quote; turns "lock 3 testimonials" into a one-click review instead of a
  from-scratch outreach task. Doesn't do the outreach — still CEO-owned — just removes the build work.
- **Ambassador referral link generator** (S, new) — auto-generates personalized `?src=ambassador_<name>`
  links + a referral count on the CEO board, so ambassadors have working, trackable links the moment
  the GTM push starts.
- **Proactive timing intelligence** (M, carried) — fire check-ins around each user's habitual active hour.
Decision: none promoted to today's build — today's capacity went to the overdue demo script instead
(see below). Both new ideas are queued next, sized to unblock the CEO's own backlog items.

## 📋 Board
- No 'todo' tasks assigned to the team — all team-owned cards are `done`. The three CEO-owned cards
  (Plan gating+Stripe, Lock 3 testimonials, Start 30-day GTM push) remain `todo`, unchanged since
  2026-06-30 (now 5 days). Reminder only — not something the team can move further.

## 🎯 The idea we're working on
Keep the trust wedge compounding at every product moment (today: memory export) while finally
converting Sage's "we're demo-ready" recommendation into an actual leave-behind investor asset.

## 📌 Goals
D7 retention ≥18% (Aug 2026 cohort); 500 registered users, ≥60% activation, ≥3% free→paid — all
still pending real traffic (GTM push) and Stripe wiring, both CEO-owned.

## ✅ Shipped today
- **Memory Vault data-export button** (`pages/memory.js`) — a "⬇ DOWNLOAD EVERYTHING RICO REMEMBERS"
  button, visible to signed-in users with vault data, builds a JSON export (per-friend message counts,
  traits, last-talked date, voice-note memories) client-side from already-fetched data and triggers a
  browser download — no new API endpoint. `next build` passes; runtime smoke (`next start` + curl
  `/memory`) confirmed clean. Committed on branch `feature/memory-data-export`, pushed to GitHub.
- **Investor demo script + 2-min walkthrough** (`standups/DEMO_SCRIPT.md`) — a beat-by-beat script
  (problem → proactive check-in → memory spotlight → data-export/trust → business model → the ask)
  plus a Reel production spec (screen-record + 1 ElevenLabs VO pass, 16:9 investor cut + 9:16 social
  cutdown). Doc only, no deploy needed.
- Retention benchmark added to `STRATEGY.md` (Character.AI D30 vs Rico's D7 target, see Strategy above).

## 📣 Content & social — Echo
- New draft: "Download everything Rico remembers" (Instagram, src=ig5) — ties to today's Memory Vault
  ship. Added to `standups/CONTENT_CALENDAR.md`. Still 📝 DRAFT, not approved to post.

## 🎬 Video — Reel
- New spec: "Yours to keep" (9:16, ~14s) for the data-export post — screen-record + 1 ElevenLabs VO,
  not generated (gated).
- New spec: "Rico, in 2 minutes" investor walkthrough (16:9 + 9:16 cutdown, ~2:00 / ~45s) inside
  `standups/DEMO_SCRIPT.md` — mostly a live screen-record; only the VO pass is gated spend.

## 📊 Metrics
No new production traffic since 2026-07-02 (nothing deployed since the 07-01 landing trust section).
Two feature branches (`feature/signup-trust-badge`, `feature/memory-data-export`) and today's demo
script are all ready to go the moment the CEO acts on deploy or GTM.

## 🚧 Challenges
- Repo working tree is on `safety/working-tree-2026-06-30` with `pages/landing.js` still holding an
  uncommitted diff — this matches the already-deployed 07-01 trust section (prod is source of truth
  via Vercel CLI, not git), so it's expected, not a new issue.
- `gh` CLI still isn't authenticated in this environment — both open feature branches are pushed but
  have no PR opened yet.

## ⚠️ Needs your approval
1. **Deploy `feature/signup-trust-badge`** to production (sign-up trust badge, code complete 2026-07-02).
2. **Deploy `feature/memory-data-export`** to production (Memory Vault data-export button, code complete 2026-07-04).
3. **Record/generate the 2-min investor walkthrough video** (`standups/DEMO_SCRIPT.md`) — mostly a live
   screen-record; the ElevenLabs voiceover pass is the only gated spend (unless you'd rather narrate it yourself).
4. REMINDER (unchanged 5 days): Plan gating + Stripe, Lock 3 named testimonials, Start 30-day GTM push
   — these are CEO-owned and are now the actual bottleneck to an investor-ready metric story.

## 🔜 Tomorrow's plan
- If the CEO unlocks any of Stripe/testimonials/GTM, that becomes top priority instead of new features.
- Otherwise: build the testimonial-capture flow or the ambassador referral-link generator (both sized
  to remove build work standing in front of the CEO's own backlog items) or proactive timing intelligence.

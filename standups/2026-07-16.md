# 🦸 Rico Daily Standup — 2026-07-16 (Thursday)
**To:** CEO (Muzaffar) · **From:** Atlas, on behalf of the team

## TL;DR
No runs 2026-07-14/15 (gap in the schedule). Today: shipped retention-lever attribution — `/api/stats`
can now rank the 4 already-deployed retention levers (check-in, "missed you," memory spotlight, streak)
by returned/D7 rate. No new CEO board tasks; the three CEO-owned items are now 16 days stale. Sage's
take: this closes the last piece of *measurement* prep — the GTM push is now purely a distribution
problem, nothing left to build first.

## 🧭 Strategy — Sage
Same flag, now 16 days: the three CEO-owned board tasks (Stripe, testimonials, GTM push) are unchanged
since 2026-06-30. Not repeating the ask with new words — instead, today's build is the team's own answer
to its own previous flag: rather than shipping a 6th blind retention lever, `/api/stats` can now rank the
4 already-deployed levers by returned/D7 rate the moment real users exist. That means GTM push isn't just
"the cheapest of the three to unblock" anymore — it's the one piece of missing data every other roadmap
decision downstream of it (which lever to double down on, which to cut) is waiting on. Nothing left to
build to strengthen that case; it's now purely a distribution problem, not a measurement one.

## 🔬 R&D — Nova
Two new ideas, both small follow-ons to today's build:
1. **Digest attribution follow-up** (XS) — the moment `feature/weekly-memory-digest` deploys, add a
   `digest_shown` track event to its component (same one-line pattern used for the memory spotlight today)
   so all 5 retention levers show up in `retention_by_lever`, not 4. Do it same-day as that deploy.
2. **Founder board cohort view** (S) — once `retention_by_lever` has real numbers, surface a tiny
   read-only summary on `/board` so the CEO sees "which lever wins" without pulling `/api/stats` JSON by
   hand. Not blocking — no data to show until real GTM traffic exists.
Promoted neither today (both depend on things that haven't happened yet: a deploy, and real traffic).

## 📋 Board
No 'todo' tasks assigned to the team — all team-owned cards remain `done`. The three CEO-owned cards are
unchanged since 2026-06-30 (now 16 days): Plan gating + Stripe, Lock 3 named testimonials, Start 30-day
GTM push.

## 🎯 The idea we're working on
Retention-lever attribution — Nova's top 2026-07-13 idea, promoted since no CEO board task outranked it.

## 📌 Goals
D7 retention ≥18% (Aug 2026 cohort); 500 registered users, ≥60% activation, ≥3% free→paid — all still
pending real traffic (GTM push, CEO-owned) and Stripe wiring (CEO-owned).

## ✅ Shipped today
- **Retention-lever attribution**: `pages/api/track.js`'s `checkin_shown`/`checkin_reply` events now
  accept an optional `variant` field so the "missed you" lapse-reengagement variant is counted separately
  from a normal check-in; a new `spotlight_shown` event does the same for the living-memory panel.
  `components/ProactiveCheckin.js` passes the variant on both existing track calls (no new fetch —
  `data.lapsed` was already available client-side); `components/MemorySpotlight.js` fires
  `spotlight_shown` once real memory items actually render, mirroring the check-in impression pattern.
  `pages/api/checkin.js` exports its existing streak-math helper (was private) so `/api/stats.js` can
  reuse it instead of duplicating; `/api/stats.js` now returns `retention_by_lever` — for each of the 4
  built levers, the users who saw it plus their returned/D7 rate, directly comparable against the overall
  base rate. Weekly digest (the 5th lever) isn't included — it's still on its own undeployed branch,
  nothing to attribute until it ships.
- `next build` passes clean. Could not runtime-verify against real Supabase data locally — this
  environment's `SUPABASE_SERVICE_ROLE_KEY` is blank in `.env.local` (checked by value *length* only,
  never printed), the same category of gap as the long-flagged blank `CLERK_SECRET_KEY`; `/api/stats` and
  `/api/track` both correctly fell back to their existing "not configured" response, unchanged from
  before today's edit, confirming this is a pre-existing local-env gap and not a regression.
- Committed to new branch `feature/retention-lever-attribution`, pushed. Folded (clean fast-forward, no
  conflicts) into `safety/working-tree-2026-06-30` so the working tree matches, also pushed.

## 📣 Content & social — Echo
No new draft today — an impression-tracking change has no visible UI, so nothing screen-recordable to
pair it with (same call as the streak-counter and digest days). The Club Feed launch post (src=ig6,
2026-07-05) is still sitting unapproved in `CONTENT_CALENDAR.md`.

## 🎬 Video — Reel
Nothing new today.

## 📊 Metrics
No new production traffic to report. Still no real GTM traffic to measure.

## 🚧 Challenges
- Two-day gap in the run schedule (no standup 2026-07-14 or 2026-07-15) — flagging in case the CEO
  expected daily coverage and something's off with the scheduler.
- Same local-dev gap category as recent runs, now confirmed to also cover Supabase: this sandboxed
  `.env.local`'s `SUPABASE_SERVICE_ROLE_KEY` is blank alongside the already-known blank
  `CLERK_SECRET_KEY`, so DB-backed routes can only be build/curl-verified against the "not configured"
  fallback locally, not against real data, until a real key is dropped in.
- Self-correction: while diagnosing the above, this run ran `grep`/`head` directly against `.env.local`,
  which put raw secret values (`STATS_KEY`, `ANTHROPIC_API_KEY`) into this session's own tool output —
  not into any standup or commit, and no external exposure, but sloppier than necessary (checking
  variable *presence*/*length* instead would have been enough). Noting it so future runs check env
  values without ever `cat`/`grep`-ing them in full. Worth confirming the `ANTHROPIC_API_KEY` currently
  in `.env.local` is the already-rotated one from the prior exposure incident, not the old one.

## ⚠️ Needs your approval
1. **New: Deploy `feature/retention-lever-attribution`** (per-lever impression tracking + `/api/stats`
   ranking, code complete 2026-07-16) to production. Lowest-risk item in this queue — instrumentation
   only, no visible UI change, additive fields on the existing meta-row stats object.
2. Carried forward: **Deploy `feature/weekly-memory-digest`** (weekly recap card on the Me tab, code
   complete 2026-07-13).
3. Carried forward: **Verify + deploy `security/idor-auth-guard`** — sign in once after deploy and
   confirm the app still works normally. If everything 403s, the Clerk cookie isn't resolving — roll
   back immediately.
4. Carried forward: **Deploy `feature/signup-trust-badge`** and **`feature/memory-data-export`** to
   production (both code-complete, now over two weeks untouched).
5. Carried forward: **Record/generate the ~2:30 investor walkthrough video** (`DEMO_SCRIPT.md`).
6. Carried forward: **Approve the Club Feed launch post** (`CONTENT_CALENDAR.md`, src=ig6) if you want
   it live.
7. REMINDER (unchanged 16 days): Plan gating + Stripe, Lock 3 named testimonials, Start 30-day GTM push —
   CEO-owned, the actual bottleneck to an investor-ready metric story.

## 🔜 Tomorrow's plan
- If any of Stripe/testimonials/GTM unlock, that becomes top priority instead.
- Otherwise, next build candidate: first-session "proof moment" tour (Nova, 2026-07-13 backlog #2) — the
  next-smallest queued idea, since retention attribution (#1) shipped today.

✅ Email alert sent to the CEO via Gmail (Zapier connector), message id `19f6ba54ce7651ac`.

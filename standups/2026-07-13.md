# 🦸 Rico Daily Standup — 2026-07-13 (Monday)
**To:** CEO (Muzaffar) · **From:** Atlas, on behalf of the team

## TL;DR
Full weekday build. Shipped the in-app weekly memory digest ("Your week with Rico" recap card on the
Me tab) — code complete, awaiting deploy. No new CEO board tasks; the three CEO-owned items are now
13 days stale. Sage is flagging a new angle: we now have 5 retention levers built and zero data on
which ones work.

## 🧭 Strategy — Sage
Same flag, now 13 days: the three CEO-owned board tasks (Stripe, testimonials, GTM push) are unchanged
since 2026-06-30. Today's build is again a retention lever aimed at users already in the funnel — four
recent builds (streak counter, club nudge, digest, plus the still-undeployed trust badge/data-export)
all point at the same side of the funnel while the top stays empty. GTM push remains the cheapest of
the three to unblock (zero engineering dependency, `?src=` tracking has been live since 2026-06-29) and
every fundraising-readiness item depends on it for real data. New angle: Rico now has FIVE built
retention levers (proactive check-in, "missed you," memory spotlight, streak counter, weekly digest)
stacked before a single cohort has been measured against any of them — once GTM starts, worth spending
a cycle instrumenting which of the five actually moves D7 before building a sixth.

## 🔬 R&D — Nova
Three new ideas, all sized S, all direct responses to today's strategy flag:
1. **Retention-lever attribution** — tag impressions/interactions per retention surface in `track.js` so
   `/api/stats` can rank the 5 existing levers by D7 correlation once real traffic exists. De-risks the
   next feature build being another blind lever.
2. **First-session "proof moment" tour** — all 5 levers need conversation history to say anything real,
   so a brand-new signup's first session is empty of them. A one-time skippable 3-card intro (sample
   check-in + sample memory chip, clearly marked preview) shows the bet on day 0. Targets the ≥60%
   activation-in-24h goal.
3. **Shareable weekly digest card** — let a user export today's new "Your week with Rico" recap as a
   redacted, no-PII image for their own Story/WhatsApp. A soft, opt-in growth loop riding on content
   that already exists.
Promoted #1 to "in progress" next (see Product Log §4) since it directly serves today's strategy flag.

## 📋 Board
No 'todo' tasks assigned to the team — all team-owned cards remain `done`. The three CEO-owned cards
are unchanged since 2026-06-30 (now 13 days): Plan gating + Stripe, Lock 3 named testimonials, Start
30-day GTM push.

## 🎯 The idea we're working on
In-app weekly memory digest — smallest already-queued item from Nova's 2026-07-09 backlog, per the
2026-07-12 "tomorrow's plan" (no CEO board tasks were open to prioritize instead).

## 📌 Goals
D7 retention ≥18% (Aug 2026 cohort); 500 registered users, ≥60% activation, ≥3% free→paid — all still
pending real traffic (GTM push, CEO-owned) and Stripe wiring (CEO-owned).

## ✅ Shipped today
- **In-app weekly memory digest**: `pages/api/digest.js` reuses the same "extract real, honest
  highlights" approach as `remembers.js` (Pillar #2), but scoped to conversations touched in the last 7
  days and framed as a past-tense recap (2-5 items, e.g. "Told Tony about the interview offer"). Cached
  7 days on the meta row under a separate `traits.digest` key so it doesn't collide with `remembers`'s
  own cache. `components/WeeklyDigest.js` renders a "Your week with Rico" card (same
  render-nothing-until-real-content pattern as `MemorySpotlight`), wired into the Me tab in
  `pages/index.js` right below the memory spotlight.
- `next build` passes. `next start` + curl smoke clean. **Could not runtime-verify in a signed-in
  browser session** — same pre-existing gap flagged in the 2026-07-06/07/09/12 standups
  (`.env.local`'s `CLERK_SECRET_KEY` is blank, so Clerk auth no-ops locally); confirmed the unauthenticated
  curl response matches `remembers.js`'s existing behavior under the same condition, so this is the known
  gap, not a regression from today's change.
- Committed to new branch `feature/weekly-memory-digest`, pushed.

## 📣 Content & social — Echo
No new draft today — a Me-tab recap card isn't a strong new screen-recordable moment on its own (same
call as the 2026-07-12 streak-counter day). The Club Feed launch post (src=ig6, 2026-07-05) is still
sitting unapproved in `CONTENT_CALENDAR.md`.

## 🎬 Video — Reel
Nothing new today.

## 📊 Metrics
No new production traffic to report. Still no real GTM traffic to measure.

## 🚧 Challenges
Same local-dev Clerk gap as the last several runs (blank `CLERK_SECRET_KEY` in `.env.local`) — every
Clerk-gated route no-ops/500s locally depending on path, so this and the last several feature branches
can only be build/curl-verified, not click-through verified, until a real (even throwaway) key is
dropped in.

## ⚠️ Needs your approval
1. **New: Deploy `feature/weekly-memory-digest`** (weekly recap card on the Me tab, code complete
   2026-07-13) to production. Low-risk (additive card + a new cached AI-summary field), same
   unverified-in-a-signed-in-session caveat as the items below.
2. Carried forward: **Verify + deploy `security/idor-auth-guard`** — sign in once after deploy and
   confirm the app still works normally. If everything 403s, the Clerk cookie isn't resolving — roll
   back immediately.
3. Carried forward: **Deploy `feature/signup-trust-badge`** and **`feature/memory-data-export`** to
   production (both code-complete, now going on two weeks untouched).
4. Carried forward: **Record/generate the ~2:30 investor walkthrough video** (`DEMO_SCRIPT.md`).
5. Carried forward: **Approve the Club Feed launch post** (`CONTENT_CALENDAR.md`, src=ig6) if you want
   it live.
6. REMINDER (unchanged 13 days): Plan gating + Stripe, Lock 3 named testimonials, Start 30-day GTM push —
   CEO-owned, the actual bottleneck to an investor-ready metric story.

## 🔜 Tomorrow's plan
- If any of Stripe/testimonials/GTM unlock, that becomes top priority instead.
- Otherwise, next build candidate: retention-lever attribution (Nova, today's #1 R&D idea) — instrument
  the 5 existing levers before adding a 6th.

✅ Email alert sent to the CEO via Gmail (Zapier connector now working again — the connection-lost
error from 2026-07-12 is resolved as of today), message id `19f5d33f2c731e92`.
